name: vSIGMA Learning Evidence Closer

on:
  workflow_dispatch:
    inputs:
      date:
        description: "Fecha YYYY-MM-DD. Vacío = hoy en Atlantic/Canary"
        required: false
        default: ""
  schedule:
    - cron: "20 1 * * *"
      timezone: "Atlantic/Canary"

permissions:
  contents: write
  issues: write

concurrency:
  group: vsigma-learning-evidence-closer-main
  cancel-in-progress: false

jobs:
  build-learning-evidence-closer:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Decide evidence date
        shell: bash
        run: |
          DATE_INPUT="${{ github.event.inputs.date }}"
          TODAY="$(TZ=Atlantic/Canary date +%F)"

          if [ -z "$DATE_INPUT" ]; then
            DATE_INPUT="$TODAY"
          fi

          echo "VSIGMA_DATE=$DATE_INPUT" >> "$GITHUB_ENV"
          echo "Learning evidence closer date: $DATE_INPUT"

      - name: Build learning evidence closer
        shell: bash
        run: |
          python scripts/build_learning_evidence_closer.py \
            --date "$VSIGMA_DATE" \
            --timezone Atlantic/Canary

      - name: Upload learning evidence artifacts
        uses: actions/upload-artifact@v4
        with:
          name: vsigma-learning-evidence-closer-${{ env.VSIGMA_DATE }}-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            data/processed/today/${{ env.VSIGMA_DATE }}/vsigma_learning_evidence_closer.*
            data/processed/governance/vsigma_learning_evidence_closer.*

      - name: Commit learning evidence outputs
        shell: bash
        run: |
          git config user.name "vSIGMA GitHub Actions Bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git status --short

          git add \
            data/processed/today/${VSIGMA_DATE}/vsigma_learning_evidence_closer.* \
            data/processed/governance/vsigma_learning_evidence_closer.* \
            2>/dev/null || true

          if git diff --cached --quiet; then
            echo "No learning-evidence output changes to commit."
            exit 0
          fi

          git commit -m "vSIGMA learning evidence closer ${VSIGMA_DATE} [skip ci]"
          git push origin HEAD:main
