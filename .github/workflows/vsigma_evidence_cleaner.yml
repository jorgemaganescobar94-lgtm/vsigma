name: vSIGMA Evidence Cleaner

on:
  workflow_dispatch:
    inputs:
      date:
        description: "Fecha YYYY-MM-DD. Vacío = hoy en Atlantic/Canary"
        required: false
        default: ""
  schedule:
    # 00:40 Atlantic/Canary - daily evidence cleaning plan after data quality governor
    - cron: "40 0 * * *"
      timezone: "Atlantic/Canary"

permissions:
  contents: write
  issues: write

concurrency:
  group: vsigma-evidence-cleaner-main
  cancel-in-progress: false

jobs:
  build-evidence-cleaner:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Decide evidence-cleaner date
        shell: bash
        run: |
          DATE_INPUT="${{ github.event.inputs.date }}"
          TODAY="$(TZ=Atlantic/Canary date +%F)"

          if [ -z "$DATE_INPUT" ]; then
            DATE_INPUT="$TODAY"
          fi

          echo "VSIGMA_DATE=$DATE_INPUT" >> "$GITHUB_ENV"
          echo "Evidence cleaner date: $DATE_INPUT"

      - name: Build evidence cleaner
        shell: bash
        run: |
          python scripts/build_evidence_cleaner.py \
            --date "$VSIGMA_DATE" \
            --timezone Atlantic/Canary

      - name: Upload evidence cleaner artifacts
        uses: actions/upload-artifact@v5
        with:
          name: vsigma-evidence-cleaner-${{ env.VSIGMA_DATE }}-${{ github.run_id }}
          if-no-files-found: warn
          retention-days: 14
          path: |
            data/processed/today/${{ env.VSIGMA_DATE }}/vsigma_evidence_cleaner.*
            data/processed/governance/vsigma_evidence_cleaner.*

      - name: Commit evidence cleaner outputs
        shell: bash
        run: |
          git config user.name "vSIGMA GitHub Actions Bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git status --short

          git add \
            data/processed/today/${VSIGMA_DATE}/vsigma_evidence_cleaner.* \
            data/processed/governance/vsigma_evidence_cleaner.* \
            2>/dev/null || true

          if git diff --cached --quiet; then
            echo "No evidence-cleaner output changes to commit."
            exit 0
          fi

          git commit -m "vSIGMA evidence cleaner ${VSIGMA_DATE} [skip ci]"
          git push origin HEAD:main
