name: vSIGMA Autonomous Improvement Advisor

on:
  workflow_dispatch:
    inputs:
      date:
        required: false
        default: ""
  schedule:
    - cron: "25 8 * * *"

permissions:
  contents: write

concurrency:
  group: vsigma-autonomous-improvement-advisor-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
          token: ${{ github.token }}
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Decide date
        shell: bash
        run: |
          D="${{ github.event.inputs.date }}"
          if [ -z "$D" ]; then D="$(TZ=Atlantic/Canary date +%F)"; fi
          echo "VSIGMA_DATE=$D" >> "$GITHUB_ENV"
      - name: Build autonomous improvement advisor
        shell: bash
        run: python scripts/build_autonomous_improvement_advisor.py --date "$VSIGMA_DATE" --timezone Atlantic/Canary
      - name: Commit advisor outputs
        shell: bash
        run: |
          git config user.name "vSIGMA GitHub Actions Bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/processed/today/${VSIGMA_DATE}/vsigma_autonomous_improvement_advisor.* data/processed/today/${VSIGMA_DATE}/vsigma_improvement_issue_* data/processed/governance/vsigma_autonomous_improvement_advisor.* data/processed/governance/vsigma_improvement_issue_* 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No advisor changes to commit."
            exit 0
          fi
          git commit -m "vSIGMA autonomous improvement advisor ${VSIGMA_DATE} [skip ci]"
          git push origin HEAD:main
