name: vSIGMA Shadow Registry Proposals

on:
  workflow_dispatch:
    inputs:
      date:
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
          token: ${{ github.token }}
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Decide date
        shell: bash
        run: |
          D="${{ github.event.inputs.date }}"
          if [ -z "$D" ]; then D="$(TZ=Atlantic/Canary date +%F)"; fi
          echo "VSIGMA_DATE=$D" >> "$GITHUB_ENV"
      - name: Build report
        shell: bash
        run: python scripts/build_shadow_registry_proposals.py --date "$VSIGMA_DATE" --timezone Atlantic/Canary
      - name: Commit report
        shell: bash
        run: |
          git config user.name "vSIGMA GitHub Actions Bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/processed/today/${VSIGMA_DATE}/vsigma_shadow_registry_proposals.* data/processed/governance/vsigma_shadow_registry_proposals.* 2>/dev/null || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "vSIGMA shadow registry proposals ${VSIGMA_DATE} [skip ci]"
          git push origin HEAD:main
